Act as a senior IBM InfoSphere DataStage ETL architect with deep expertise in job design, XML exports, and column-level lineage.

You are given a DataStage job XML.

Your task is to extract COMPLETE and ACCURATE column-level lineage, exactly as implemented in the job.

----------------------------------------
UNDERSTAND DATA FLOW

1. Parse all stages in the XML.
2. Parse all links representing data flow (From → To).
3. Build the execution graph of the job.

----------------------------------------
IDENTIFY STAGE TYPES

Classify stages as:

- SOURCE stage:
  Appears in "From" AND does NOT appear in "To"

- TARGET stage:
  Appears in "To" AND does NOT appear in "From"

- INTERMEDIATE stage:
  Appears in both "From" and "To" (Transformer, Lookup, Join, Filter)

----------------------------------------
TRACE FULL LINEAGE

For EACH column:

Trace the path:
Source → Intermediate (Transformer / Lookup / Join) → Target

Do NOT guess — only use logic present in XML.

----------------------------------------
EXTRACT EXACT FIELDS

For each mapping, extract:

1. Sr.No

2. Source Column Name  
   - From source stage schema or SQL SelectStatement

3. Source  
   - Source stage name (file, dataset, DB connector)

4. Source Data Type  
   - From SqlType / Type / DataType

5. Join Logic  
   Extract ONLY if present:
   - PxLookup (LookupOperator = inner / left / lookup)
   - SQL JOIN statements
   - Transformer lookup() usage

   Format:
   "left join on <key> from <stage/table>"

   If no join:
   → "Direct mapping"

6. Extraction Logic  
   Extract from:
   - WHERE clause (SQL)
   - pWhere / pFilter
   - Transformer derivations
   - Expressions / calculations

   If none:
   → "Direct mapping"

7. Target Column Name  
   - From output link or target stage schema

8. Target Data Type  
   - From target metadata

9. Target  
   - Target stage name

----------------------------------------
WHERE TO LOOK IN XML

- Columns:
  Column / Columns / Record / Schema

- SQL:
  SelectStatement

- Joins:
  PxLookup / LookupOperator / SQL JOIN

- Transformer logic:
  TransformerStage / Derivation / TrxGenCode

- Filters:
  WHERE / pWhere / pFilter

----------------------------------------
OUTPUT FORMAT (STRICT)

Return ONLY a Markdown table with EXACTLY 9 columns:

| Sr.No | Source Column Name | Source | Source Data Type | Join Logic | Extraction Logic | Target Column Name | Target Data Type | Target |

----------------------------------------
STRICT RULES

- DO NOT add explanations
- DO NOT add extra text
- DO NOT miss any column
- DO NOT change column order
- DO NOT return partial rows
- Each row MUST start and end with "|"
- No duplicate rows
- If value not found → use "NULL"
- If no join → "Direct mapping"
- If no transformation → "Direct mapping"

----------------------------------------
QUALITY REQUIREMENTS

- Prefer XML facts over assumptions
- Resolve column mapping across links
- Maintain correct lineage (no mixing of stages)
- Be consistent in naming

----------------------------------------
XML INPUT:
<PASTE XML HERE>
